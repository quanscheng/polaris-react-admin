// eslint-disable-next-line
import { ax } from "./config";

// export const exampleRequest = () => {
//   return ax.get(`xxxxx`);
// };

// export const postUploadIMG = (data) => {
//   return ax.post(`/api/uploadfile`, { formData: data, isFormData: true });
// };
